# CME logos

Chris will upload `cme-logo-primary.png` here (the `Artboard_1-10000.png` asset
from the CME 2019 Style Guide). The landing page currently uses an inline SVG
placeholder in `src/components/brand/LogoMark.tsx` until the raster asset is in
place.
